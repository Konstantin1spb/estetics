$(document).ready(function(){
  $('.first-slider').slick({
  	autoplay: true,
    autoplaySpeed: 4000
  });

  $('.new__slider').slick({
  	autoplay: true,
    autoplaySpeed: 4000,
    slidesToShow: 4,
    slidesToScroll: 4
  });

  $('.week__slider').slick({
  	autoplay: true,
    autoplaySpeed: 4000,
    slidesToShow: 4,
    slidesToScroll: 4
  });
});